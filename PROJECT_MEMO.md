# プロジェクト覚書 — サボテンEC情報集約データベース(cactus-db)

就職活動のポートフォリオ用に、企画から実装・保守運用までの開発フロー全体を振り返ってまとめたもの。
「何を」だけでなく「なぜそう判断したか」を中心に記録している。

---

## 1. プロジェクト概要

### 1.1 何を作ったか

サボテン(Cactaceae科)のうち、ワシントン条約(CITES)附属書I種、およびIUCNレッドリストで
Critically Endangered(近絶滅種)に指定されている種について、

1. **保護種マスターデータベース**を構築し(学名表記のゆれを吸収して判定できる形で)、
2. 実在する複数の海外サボテン専門ECサイト・種子頒布サイトをスクレイピングし、
3. 「どの保護種が、どのサイトで、いくらで、購入可能か」を横断的に検索できる

静的Webページを作った。個人の学習・調査目的のプロジェクトであり、営利目的の再配布は行っていない。

### 1.2 最終成果物(数字で見る規模)

| 項目 | 値 |
|---|---|
| マスターデータの登録種数(属単位含む) | 174件 |
| うちCITES・IUCN両方に該当する種(`source='BOTH'`) | 11件 |
| 対応した商品スクレイピング対象サイト数 | 11サイト |
| 検討したが対象外とした・保留したサイト数 | 5サイト |
| 累計スクレイピング商品数 | 約23,700件 |
| うち保護種と確定マッチした商品(`confirmed`) | 2,208件 |
| 購入可能なサイトが1つ以上見つかった保護種数 | 121種 / 174種 |
| 自動テスト件数 | 14件(すべてpass) |

### 1.3 技術スタック

- **言語/ランタイム**: Node.js 22 LTS(ESM, `"type": "module"`)
- **DB**: SQLite(`better-sqlite3`。同期APIでスクリプトが単純になるため採用)
- **スクレイピング**: 標準`fetch` + `cheerio`(HTML)、`xlsx`(SheetJS、Excel解析)、`pdf-parse`(PDF解析)
- **フロントエンド**: フレームワーク無し。素のHTML/CSS/JSのみ(依存を増やさず、ホスティングコストゼロで完結させるため)
- **テスト**: Node標準の`node:test` + `node:assert`(追加パッケージ不要)
- **IDE**: IntelliJ IDEA

---

## 2. 開発の背景と要件定義プロセス

### 2.1 発端

「保護対象のサボテンが、海外のECサイトでどれだけ流通しているかを自分で調べたい」という個人的な問題意識が出発点。
一度に全体を作るのではなく、**Phase 0(マスターDB構築)→ Phase 1(ECサイトのスクレイピングと突合)** の2段階に分けて計画した。

### 2.2 Phase 0: 要件定義の詰め方

最初に「実装指示書」という形でPhase 0の要件をまとめたドキュメントを用意し、それをそのままAIへの最初の指示として渡す、という進め方を取った。ただし要件には意図的に不明瞭な部分を残しており、実装に着手する前に以下の順で認識合わせを行っている。

1. **要件の不明点を洗い出す**(実装前レビュー): CSVの重複行の扱い、学名表記ゆれの正規化方針、ファジーマッチの優先順位、Web検索ページのCORS制約など、約15項目を実装前に列挙。
2. **実データを見てから回答する**: 質問への回答は「実際にダウンロードしたCSVを見てから」という形で、抽象論ではなく実データに基づいて意思決定した。
3. **実装 → 実データでの検証 → 想定との差分を確認**、というサイクルを回す。

この「先に疑問点を洗い出してから実装に入る」流れは、Phase 1(サイト追加のたびに実データを調査してから実装する)でも一貫して踏襲した。

### 2.3 Phase 1: スコープの決め方

ECサイトのスクレイピングは対象サイトが16件と多く、技術方式もサイトごとにバラバラ(Shopify JSON API、静的HTML、Excel配布、PDF配布、SPA)だったため、**最初から16サイト全部を実装しようとせず、技術方式の異なる代表サイトでパイプライン全体(取得→学名照合→表示)を通してから、残りを段階的に追加する**方針を最初に確認してから着手した。結果的にこれが功を奏し、後から見つかった学名照合ロジックのバグ(4章参照)を、全サイトに影響が及ぶ前に低コストで発見・修正できた。

---

## 3. システムアーキテクチャ

```
data/source/*.csv (CITES附属書I、IUCN分類、IUCN評価)
        │  npm run import:species
        ▼
data/cactus.db (SQLite)
  ├─ species_master   … 保護種マスター(属/種/亜種、CITES附属書、IUCNカテゴリ)
  ├─ species_synonym  … 学名の異名(シノニム)
  └─ products         … スクレイピングした商品(学名照合結果つき)
        │
        │  npm run scrape (11サイトのスクレイパーを順次実行)
        │  → 各商品を matchSpecies() で species_master と照合し products に保存
        │
        │  npm run export:species
        ▼
public-data/species_master.json (静的JSON、products由来のlistingsを埋め込み済み)
        │  fetch (ブラウザ)
        ▼
web/species-search.html + species-search.js (検索UI、サーバー不要)
```

設計上のポイントは、**Web側は完成済みの静的JSONを読むだけ**にしたこと。検索・絞り込みはすべてクライアントサイドの
`Array.prototype.filter`で完結させており、サーバーサイドの実行環境を必要としない(IntelliJの簡易サーバーか、静的ホスティングがあれば動く)。

---

## 4. 技術的な要点と設計判断

### 4.1 データベース設計(`db/schema.sql`)

- `species_master`: 属(GENUS)単位・種(SPECIES)単位・亜種(SUBSPECIES)単位のいずれの粒度の登録も1つのテーブルで表現できるよう`rank`列で区別。CITES附属書の属単位登録は「属内の全種に法的効力が及ぶ」という実際の制度を反映するため、意図的にこの設計にした。
- `products.species_id`は`species_master(id)`への外部キー。ここで一度落とし穴があり(7章参照)、`import:species`実行時に`products`も一緒にクリアするよう後から修正している。
- `species_synonym`、`products`とも`UNIQUE`制約を貼り、再実行時に重複が積み上がらないようにしている(冪等性の確保)。

### 4.2 学名照合ロジック(`scripts/lib/`)

これがこのプロジェクトの中核ロジック。スクレイピングした商品名(表記ゆれだらけの生テキスト)から学名を取り出し、
マスターデータと突き合わせる処理を、**優先順位付きの多段階判定**として実装した。

| 段階 | 判定内容 | ステータス |
|---|---|---|
| ① | 属+種小名の完全一致 | `confirmed` / `species` |
| ② | シノニム(異名)の完全一致 | `confirmed` / `species` |
| ②b | 旧亜種名など「属名+識別語」が離れて出現するケース(後述) | `confirmed` / `species` |
| ③ | 属単位のCITES登録(GENUS rank) | `confirmed` / `genus` |
| ④ | 編集距離によるファジーマッチ(タイプミス救済) | `needs_review` / `fuzzy` |

**設計思想として「inclusive(拾い漏れを避ける)」方針を明確にした。** 属単位のCITES登録は法的に属内全種へ及ぶため②と③の間に優先順位を設け、変種・亜種表記(`var.`, `subsp.`など)は種レベルへ意図的に畳み込むことで、学術分類と商業上の流通名の食い違いを吸収している。一方で、needs_review(ファジーマッチ)は「購入可能」と断定するリスクがあるため、**検索ページ上には表示せず、スクレイピング実行時のログにのみ出力する**という線引きも行った。

### 4.3 スクレイパーのアーキテクチャ(`scripts/scrapers/`)

各サイトを1ファイル1モジュール(`{ id, name, scrape() }`を export)として実装し、`siteRegistry.js`に登録するだけで
`scrapeProducts.js`のオーケストレーターから呼び出される設計にした。共通処理(タイムアウト・リトライ・待機時間つきの
HTTP取得)は`httpClient.js`に集約し、同一基盤(Shoptet)の複数サイトは`shoptet.js`のファクトリ関数として、
Excel配布形式の複数サイトは`xlsxSeedlist.js`の共通パーサーとして横展開できるようにした。

サイトごとの技術方式は以下の4パターンに分類できた。

1. **構造化API直叩き型**(Hajek Cactus): Shopifyの`/products.json`を直接取得。最も低コスト。
2. **HTML解析型**(ADBLPS, Malej Jarda, Kakteen.cz, Rare Cacti Top): `cheerio`でDOM解析。
3. **属フィルタ型クロール**(Kaktus Köhres): 属ごとに1ページという構造のサイトで、全属(150以上)を巡回するのは非効率かつサーバーへの負荷も大きいため、**`species_master`に登録済みの属だけに絞り込んでクロールする**設計にした。これによりリクエスト数を大幅に削減しつつ目的を達成している。
4. **バイナリファイル解析型**(Kaktusy webzdarma, Cactus Moravia: xlsx / Wonderful Cactus: PDF): カタログページのHTMLから配布ファイルのURLを**都度自動検出**し(URLをハードコードしない)、ダウンロードして解析する。季節ごとにファイル名やバージョンが変わっても、スクレイパー側の修正が不要になるようにした。

---

## 5. 開発フローと時系列

### Phase 0(マスターDB構築)
1. 要件の不明点を洗い出し、実データ(CITES/IUCN CSV)を見ながら回答をもらう
2. Node.js環境構築(後述のトラブルシュートあり)
3. DBスキーマ・importスクリプト・学名正規化ロジック・exportスクリプト・検索ページを実装
4. 実データで取り込み、指示書の想定値(BOTH該当種10種前後)と突き合わせて検証
5. 実データ由来の想定外パターン(シノニム文字列内の`var.`表記、複数著者のカンマ区切りなど)を発見し、抽出ロジックを正規表現の逐次split方式からグローバルパターンマッチ方式に変更して解消

### Phase 1(ECサイトスクレイピング、9サイトを段階的に追加)
1. **代表3サイト**(Hajek Cactus / ADBLPS / Malej Jarda)でパイプライン全体を通す
2. 実データ照合中に**誤検出バグを発見**(`Pediocactus simpsonii`という非保護の普通種が、`Pediocactus bradyi`の旧亜種名シノニムに前方一致してしまい誤って保護種判定されていた)。シノニム照合を前方一致から完全一致に変更して解消
3. 残り13サイトのURL・スクレイピング方針を追加ヒアリングし、**Shoptet系2サイト・独自PHP1サイト・属フィルタ型1サイト**を追加(6サイト体制に)
4. ユーザーからの実データ指摘(「Pediocactus + bradyiが離れた位置にあるケースを拾えないか」)を受け、**属名と識別語の共起による旧亜種名マッチング**を新たに設計・追加(既存の優先順位を崩さない形で②と③の間に挿入)
5. **xlsx配布2サイト**を追加。npmレジストリ配布の`xlsx`パッケージに未修正の高深刻度脆弱性があることに気づき、SheetJS公式配布のパッチ版に切り替え
6. **PDF配布1サイト**(Wonderful Cactus)を追加。185ページ・16MBのPDFから独自フォーマットの商品リストをテキスト抽出。「まとめ売り区画」内に埋もれた売り切れ表記を見逃す不具合を、安全側に倒す形で解消
7. ユーザーからの実データ指摘(`Pediocactus knowltonii`の検索漏れ)を受けて**根本原因を調査・説明**した上で、**全サイト共通の正規化ロジックに潜んでいた非対称なバグ**(属名は記号除去、種小名は完全一致という非対称チェック)を修正。再スクレイピングで他サイトの精度向上という副次効果も確認
8. **SuccSeed(SPA)を追加検討** → 実は直接呼べるJSON-RPC APIが存在し、Playwright不要で実装できることを発見したが、そのAPIのパスが`robots.txt`で明示的に禁止(`Disallow: /backend/`)されており、ブラウザ経由でも同じ禁止パスを踏むことになるため**実装を断念**

### Phase 2(公開後の追加対応、11サイト目までを追加)
1. ユーザーからの実データ指摘(「Turbinicarpusが分類改訂でGymnocactus/Rapicactusに分かれるように、属そのものが分割・改称されたケースをスクレイピングしづらい」)を受けて調査。**照合ロジック自体は既にシノニム経由で旧属名にマッチできていた**が、Kaktus Köhresのように「属ごとに1カテゴリページ」の構造を持つサイトでは、クロール対象の属を`species_master.genus`(CITES/IUCNの現行属名)だけで絞り込んでいたため、`Gymnocactus`等の旧属名ページ自体がクロール対象から漏れていたことが根本原因と判明。`species_synonym`の先頭語(旧属名)も対象に含めるよう修正し、再スクレイピングでKöhresの取得件数が増加したことを確認
2. 10月開始の季節オファーだったため保留にしていた**Cact.cz**のPDFカタログが公開されたため、新規スクレイパーを追加。「属見出し(旧属名を括弧併記)+カタログ番号付き商品行」というテキストレイアウトを解析し、実データ上でも`GYMNOCACTUS (TURBINICARPUS)`のような旧属名見出しが1.の修正と組み合わさって正しく確定マッチすることを確認
3. Phase 1で断念していた**SuccSeed**を、ユーザーの再検討要望を受けて改めて調査。商品ページのHTMLを実際に取得して**サーバー側では商品名も価格も出力されない純粋なSPAシェル**であることを確認し、「ヘッドレスブラウザでも禁止パス`/backend/`を自動で踏む」という当初の判断が正しかったことを裏付けた。一方で**robots.txtが自ら`Sitemap:`を案内しており、そのサイトマップのURL自体に学名が含まれている**ことを発見。サイトマップ(計7リクエスト)だけを解析する方式に切り替え、**価格・在庫は禁止パス由来のため取得せずnullのまま**とすることで、robots.txtに一切抵触せず45種・98件の保護種リスティングを追加した(ただし「在庫あり」とは断定せず「価格不明/在庫不明」と表示)

---

## 6. 発生した課題と対応(トラブルシュート記録)

ポートフォリオとして特に「エンジニアリング判断」が現れている箇所を中心に記録する。

| # | 課題 | 原因 | 対応 |
|---|---|---|---|
| 1 | Node.jsが未インストール | 環境構築前提だった | winget導入 → Node 24を入れたが`better-sqlite3`のプリビルドが無くVisual Studio Build Toolsが要求される事態に。大容量インストールを避け、プリビルド提供のあるNode 22 LTSに切り替え |
| 2 | シノニム誤マッチで非保護種を保護種と誤判定 | 前方一致(`LIKE`)によるシノニム照合が、3語シノニムの先頭2語が別の実在種と一致するケースを想定していなかった | 完全一致(`=`)に変更 |
| 3 | 旧亜種名(属名+識別語が離れて出現)を拾えない | ①②の照合は「属+種小名が隣接」を前提としていた | 属名と識別語(シノニムの末尾語)の共起を見る新しい判定ステップを追加 |
| 4 | `import:species`実行時にクラッシュ | `products`テーブルの外部キー制約により、`species_master`の全削除ができなかった(Phase 0設計時にはPhase 1のテーブルとの整合性を考慮していなかった) | `species_master`再構築時は`products`も一緒にクリアする設計に変更 |
| 5 | スクレイピングが数十分ハングしたように見えた | 個々のHTTPリクエストにタイムアウトが設定されておらず、進捗ログも無かったため「遅い」のか「止まっている」のか判別できなかった | タイムアウト(20秒)+1回リトライを追加。ページ単位の進捗ログも追加し、以後は正常な低速処理と本当のハングを区別できるようにした |
| 6 | 通貨表示が実行のたびにUSD/JPYで変動 | Shopifyのフロントエンド表示通貨はアクセス元の地域推定に依存し、products.jsonの生の価格とは独立していた | 誤った通貨を断定表示するより「(通貨要確認)」と正直に表示する方を選択 |
| 7 | xlsxパーサーに高深刻度の脆弱性 | npmレジストリに公開されている`xlsx`パッケージが、SheetJS社の方針転換で無期限に未パッチのまま | SheetJS公式CDN配布の修正済みビルドに依存関係を切り替え |
| 8 | PDF内の「まとめ売り区画」で売り切れ商品を見逃す | 区画内の個別品目が独自の番号付きリストで区切られており、区画全体を1エントリとして扱う都合上、区画先頭のフラグだけでは区画内の一部品目の売り切れ表記を検知できなかった | 個別品目への安全な分割が困難なため、区画内のどこかに売り切れ表記があれば区画全体を安全側に倒して除外する方針に変更 |
| 9 | `Pediocactus knowltonii`の検索漏れ(ユーザー指摘) | 学名正規化処理で、属名は記号除去してから判定するのに種小名は「完全に文字だけ」の厳密一致でしか採用しない、という非対称な実装になっており、種小名の直後に空白なしでカンマが続くケース(英語の標準的な句読法)で判定が丸ごとスキップされていた | 種小名側も属名と同様に記号除去してから判定するよう修正。回帰防止のテストケースを追加 |
| 10 | SuccSeedの直接APIがrobots.txtで禁止 | 直接叩けるJSON-RPC APIを発見したが、そのパスが`Disallow: /backend/`の対象だった | ブラウザ経由でも同じ禁止パスに到達するため、APIの利用は断念。後日Phase 2で、robots.txtが自ら案内する`sitemap.xml`のURLに学名が含まれることを見つけ、**禁止パスを一切踏まずに「取り扱い学名」だけを取得する**方式で実装(価格・在庫は取得できないためnull) |
| 11 | 属分割・改称された旧属名のカテゴリページがクロールされない(ユーザー指摘) | クロール対象の属を`species_master.genus`(CITES/IUCNの現行属名)のみで絞り込んでおり、`species_synonym`に登録済みの旧属名(例: `Gymnocactus`)がクロール対象の入口段階で漏れていた。照合ロジック自体は問題なかった | `species_synonym`の先頭語(旧属名部分)も対象属集合に含めるよう`getTargetGenera`を拡張 |

---

## 7. 倫理・法令面での判断

スクレイピングという性質上、技術的な可否だけでなく「やっていいか」の判断を都度行った。方針は一貫して
**「技術的に可能でも、サイト側の意思表示(robots.txt等)を優先する」**。

- 全サイトで事前に`robots.txt`を確認し、禁止パスにはアクセスしない
- 識別可能なUser-Agentを名乗り、リクエスト間に待機時間を設定
- **Kakteen-Haage**: `robots.txt`が`User-agent: ClaudeBot`を名指しで`Disallow: /`としていたため、名指しでの拒否を理由に対象外とした
- **Uhlig Kakteen**: アクセス時にボット検出用のJS Proof-of-Workチャレンジが挟まっており、これを解読・回避する実装は行わない方針とした
- **SuccSeed**: 直接叩けるAPIを発見したが`robots.txt`の禁止パス(`/backend/`)に該当するため、APIもヘッドレスブラウザも使わない判断をした。代わりに**robots.txtが自ら案内している`sitemap.xml`だけ**を参照し、URLに含まれる学名から「取り扱い種」を判定している。禁止パスからしか得られない価格・在庫は取得せずnullとし、画面上も「価格不明/在庫不明」と表示する(6章参照)。なお、公開されていないフィードURL等の当てずっぽうな探索は方針に反するため行っていない
- **Malej Jarda**: `robots.txt`に「検索エンジンへのインデックスは許可するがAI学習データとしての収集は禁止する」という`Content-Signal`があった。今回の用途はAI学習ではなく個人の商品カタログ照合と判断して実施したが、この判断の是非はユーザー自身の意思を確認する形にした(結果、続行の了承を得ている)

---

## 8. テスト戦略

`node:test`を使い、`scripts/lib/speciesMatcher.js`の判定ロジックを中心に検証している。特徴は
**モックではなく実データ(実際に取り込んだCITES/IUCN由来のDB)に対してテストする**という方針を最初に確認した上で採用したこと。

最終的なテストケース(12件)は、実装時の想定ケースだけでなく、**実運用で見つかったバグの回帰防止**としても追加されている。

- 完全一致・シノニム一致・属単位マッチ・ファジーマッチという基本4パターン
- CITES/IUCN両方の情報統合(`source='BOTH'`)
- 変種表記の畳み込み
- 保護対象外の種を誤検出しないこと(false positiveの防止)
- 旧亜種名(属名+識別語の共起)のマッチングと、その悪用(誤検出)防止
- 種小名直後にカンマが続くケース(実データで発見したバグの回帰テスト)

---

## 9. 運用・保守

日常的な運用手順は[SETUP.md](SETUP.md)に集約している。要点のみ以下に記す。

- **通常の更新サイクル**: `scrape products` → `export species` の順に再実行するだけで、価格・在庫情報が最新化される(CSVの再取り込みは不要)
- **CITES/IUCNデータの更新時**: `import species` → `export species` の順。`import species`は`species_master`のIDを振り直すため、整合性維持のために`products`も同時にクリアされる設計になっている(=更新後は`scrape products`のやり直しが必須)
- **既知の制約**: `needs_review`判定の商品は検索ページに表示しない、通貨が確認できない場合は断定表示しない、など「不確実な情報は隠す」方針を徹底している

---

## 10. 今後の課題

- **SuccSeed**: サイトマップ経由で「取り扱い種」までは取得済みだが、価格・在庫はrobots.txtで禁止された`/backend/`からしか得られないため未取得のまま。サイト側に非商用利用の可否と価格表提供を問い合わせる余地はある
- **Cactus Hobby**: 年末のみ注文受付のため、現時点では価格表が存在しない
- 上記以外に、`needs_review`(ファジーマッチ)判定商品を人手でレビューし、`species_synonym`テーブルに正式なシノニムとして登録していくことで、判定精度をさらに上げる余地がある

---

## 11. このプロジェクトで意識したこと(振り返り)

- **要件が曖昧な部分は、実装前に言語化して確認してから進める。** 特に「実データを見てから判断する」姿勢を最後まで貫いた結果、指示書の時点では想定していなかったパターン(シノニム内の階級語、複数著者のカンマ区切り、PDFのまとめ売り区画など)を都度発見・吸収できた。
- **「拾い漏れを避ける(inclusive)」設計と「誤って断定しない(安全側に倒す)」設計を、場面ごとに使い分けた。** 属単位のCITES登録は法的根拠があるため拾う側に倒し、通貨や在庫のように根拠が弱い情報は断定を避ける、という一貫した基準を持たせている。
- **技術的に可能かどうかと、やっていいかどうかを分けて考えた。** SuccSeedのように「実装コストが低い魅力的な近道」(JSON-RPC API)が見つかっても、サイト側の意思表示(robots.txt)を確認した上で見送る判断をしている。さらにその先で「禁止されているのは何か」を精読し、**サイト側が自ら公開を意図しているサイトマップだけを使う**という、ルールに抵触しない別経路を見つけて実装した。取れない情報(価格・在庫)は推測で埋めずnullのままにしている。
- **一度作って終わりにせず、実データでの検証とユーザーからのフィードバックを踏まえて設計を見直し続けた。** その過程で見つかったバグの多くは、単体テストだけでは気づけなかったもの(実際のスクレイピング結果を人間の目で確認して初めて気づけたもの)であり、自動テストと実データ検証を両輪で回すことの重要性を実感した。
